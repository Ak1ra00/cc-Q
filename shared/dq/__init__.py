# cc-Q: everything of ours lives under this namespace, so upstream can still be merged.
#
# Nothing here may assume a wall clock exists (see SPEC.md, Findings) and nothing
# here may touch the network, because there isn't one.

DQ_VERSION = '0.1'
